// import {assert} from 'chai';
// import {getDefaultState} from '../logic/config';
// import {changeLevel} from '../logic/change-level';

// describe(`Change level`, () => {

//   it(`should change level on 2`, () => {
//     assert.equal(changeLevel(getDefaultState(), 2).level, 2);
//   });

//   it(`should change level on 5`, () => {
//     assert.equal(changeLevel(getDefaultState(), 5).level, 5);
//   });

//   it(`should return Error if level is negative`, () => {
//     assert.throws(() => changeLevel(getDefaultState(), -1).level);
//   });

//   it(`should return Error if level is more max level`, () => {
//     assert.throws(() => changeLevel(getDefaultState(), 11).level);
//   });
// });
