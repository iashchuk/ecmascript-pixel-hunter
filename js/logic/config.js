const START_GAME = {
  level: 0,
  lives: 3,
  timer: 30
};

const MAX_LEVEL = 10;

const AnswerScore = {
  WRONG: 0,
  SLOW: 50,
  NORMAL: 100,
  FAST: 150,
  BONUS: 50
};

export {AnswerScore, START_GAME, MAX_LEVEL};
